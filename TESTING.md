# 🧪 TESTING.md

Руководство по тестированию WipeDisk Enterprise v1.3.0-stable

## 🎯 Цель тестирования

Проверить все ключевые функции WipeDisk Enterprise на различных конфигурациях Windows и убедиться в корректной работе всех режимов.

---

## 📋 Сценарии тестирования

### 🔍 **Сценарий 1: Проверка очистки системного мусора (Maintenance)**

**Цель**: Проверить корректность работы системного обслуживания

**Шаги выполнения:**
1. **Подготовка системы**
   ```cmd
   # Очистка временных файлов
   wipedisk.exe --maintenance --plan=quick_cleanup --verbose
   
   # Очистка DNS кэша
   wipedisk.exe --maintenance --plan=quick_cleanup --verbose --task=dns
   
   # Очистка корзины
   wipedisk.exe --maintenance --plan=quick_cleanup --verbose --task=recycle
   ```

2. **Проверка результатов**
   - ✅ Проверить, что временные файлы удаляются
   - ✅ Проверить, что DNS кэш очищается
   - ✅ Проверить, что корзина очищается
   - ✅ Проверить, что отчеты создаются в `reports/`
   - ✅ Проверить логирование операций

3. **Ожидаемые результаты:**
   - Все операции завершаются со статусом "SUCCESS"
   - Создаются JSON отчеты в папке `reports/`
   - Логи записываются в `logs/wipedisk.log`
   - Exit codes: 0 (успех), 1 (ошибка), 2 (предупреждения)

4. **Проверка безопасности:**
   - Запуск без прав администратора должен завершаться с ошибкой
   - Запуск на системном диске должен требовать `--allow-system-disk`
   - Проверить, что операции безопасны для системных файлов

---

### 🧪 **Сценарий 2: Затирание свободного места на тестовой флешке**

**Цель**: Проверить корректность работы затирания свободного места

**Шаги выполнения:**
1. **Подготовка тестовой среды**
   ```cmd
   # Подключить тестовую флешку (например, F:)
   # Проверить доступность
   wipedisk.exe --info
   ```

2. **Тестовое затирание**
   ```cmd
   # Тестовый режим (без реального удаления)
   wipedisk.exe --wipe F: --dry-run --verbose
   
   # Быстрое затирание (1 pass)
   wipedisk.exe --wipe F: --method=quick --verbose
   
   # Стандартное затирание (3 passes)
   wipedisk.exe --wipe F: --method=standard --verbose
   ```

3. **Проверка результатов**
   - ✅ В dry-run режиме не должно быть реального удаления
   - ✅ Прогресс должен отображаться корректно
   - ✅ Отчеты должны создаваться с детальной информацией
   - ✅ Проверить, что SSD обнаруживается корректно

4. **Ожидаемые результаты:**
   - Dry-run: симуляция операций без реального удаления
   - Quick: 1 проход затирания
   - Standard: 3 прохода затирания
   - SSD detection: предупреждения для твердотельных накопителей

5. **Проверка безопасности:**
   - Затирание системного диска требует `--allow-system-disk`
   - Проверить, что операции безопасны для флешек
   - Убедиться в невозможности затирания системных дисков по умолчанию

---

### 📊 **Сценарий 3: Проверка генерации отчетов в папке reports/**

**Цель**: Проверить корректность работы системы отчетов

**Шаги выполнения:**
1. **Создание отчетов**
   ```cmd
   # Запустить диагностику для создания отчета
   wipedisk.exe diagnose --quick --output=reports/diagnostic_test.json
   
   # Запустить затирание для создания отчета
   wipedisk.exe --wipe F: --method=quick --report
   
   # Запустить обслуживание для создания отчета
   wipedisk.exe --maintenance --plan=quick_cleanup --report
   ```

2. **Проверка управления отчетами**
   ```cmd
   # Показать все отчеты
   wipedisk.exe reports --list
   
   # Показать отчеты по типу
   wipedisk.exe reports --list --type=diagnostic
   
   # Очистка старых отчетов
   wipedisk.exe reports --cleanup --keep-days=7
   ```

3. **Проверка результатов**
   - ✅ Отчеты создаются в формате JSON
   - ✅ Отчеты отображаются корректно
   - ✅ Фильтрация по типу работает
   - ✅ Очистка старых отчетов работает

4. **Ожидаемые результаты:**
   - JSON файлы с правильной структурой
   - Автоматическая сортировка по времени создания
   - Корректная фильтрация по типам
   - Успешная очистка старых отчетов

---

### 🖥️ **Сценарий 4: Запуск через PowerShell скрипт (CLI режим)**

**Цель**: Проверить корректность работы в автоматическом режиме

**Шаги выполнения:**
1. **Создание PowerShell скрипта**
   ```powershell
   # test_wipedisk.ps1
   #!/usr/bin/pwsh
   
   # Тестирование интерактивного режима
   & "wipedisk.exe"
   
   # Тестирование CLI команд
   & "wipedisk.exe" --version
   & "wipedisk.exe" diagnose --quick
   & "wipedisk.exe" --maintenance --plan=quick_cleanup --silent
   ```

2. **Тестирование автоматического запуска**
   ```cmd
   # Запуск через PowerShell
   powershell -ExecutionPolicy Bypass -File test_wipedisk.ps1
   
   # Тестирование с параметрами
   powershell -Command "& 'wipedisk.exe' --version"
   ```

3. **Проверка результатов**
   - ✅ PowerShell скрипты работают корректно
   - ✅ Автоматический режим работает
   - ✅ Silent режим работает без интерактивных подсказок
   - ✅ Exit codes корректно обрабатываются

4. **Ожидаемые результаты:**
   - Корректная работа в PowerShell
   - Silent режим без пользовательского взаимодействия
   - Правильная обработка параметров командной строки

---

## 🛡️ **Проверки безопасности**

### 🔐 **Права администратора**
```cmd
# Тест без прав администратора
wipedisk.exe --maintenance --plan=quick_cleanup

# Ожидаемый результат: Ошибка с кодом 1
# Ожидаемое сообщение: "требуются права администратора"
```

### 🚫 **Защита от случайного удаления**
```cmd
# Тестирование защиты от случайного удаления
wipedisk.exe --wipe F: --method=standard --dry-run

# Проверка: файлы не должны быть затерты
# Ожидаемый результат: симуляция операций
```

### 🛡️ **Проверка системного диска**
```cmd
# Попытка затирания системного диска (без флага)
wipedisk.exe --wipe C:

# Ожидаемый результат: ошибка с требованием --allow-system-disk
# С флагом --allow-system-disk
wipedisk.exe --wipe C: --allow-system-disk --verbose

# Ожидаемый результат: успешное затирание с предупреждениями
```

### 🔍 **Проверка верификации качества**
```cmd
# Тестирование верификации
wipedisk.exe verify --last-session

# Тестирование глубокой верификации
wipedisk.exe verify --physical --report

# Ожидаемый результат: детальный отчет о качестве затирания
```

---

## 📋 **Тестирование на разных конфигурациях**

### 🖥️ **Windows 7**
```cmd
# Проверка совместимости с Windows 7
wipedisk.exe --version

# Тестирование операций
wipedisk.exe --maintenance --plan=quick_cleanup --verbose
```

### 🖥️ **Windows 10**
```cmd
# Тестирование оптимизаций для Windows 10
wipedisk.exe --maintenance --plan=quick_cleanup --verbose

# Проверка путей очистки
wipedisk.exe --maintenance --plan=quick_cleanup --verbose --debug-paths
```

### 🖥️ **Windows 11**
```cmd
# Тестирование оптимизаций для Windows 11
wipedisk.exe --maintenance --plan=quick_cleanup --verbose

# Проверка новых функций
wipedisk.exe diagnose --full --verbose
```

### 🏗️ **x64 vs x86**
```cmd
# Проверка архитектуры
wipedisk.exe --version

# Проверка работы на x86 (если доступно)
# Ожидаемый результат: только x64 поддерживается
```

---

## 📊 **Проверки производительности**

### 🧪 **Тестовый режим производительности**
```cmd
# Тестирование производительности очистки
wipedisk.exe --maintenance --plan=quick_cleanup --dry-run --verbose --profile=fast

# Тестирование производительности затирания
wipedisk.exe --wipe F: --method=quick --dry-run --verbose --profile=fast
```

### 📈 **Мониторинг ресурсов**
```cmd
# Тестирование с мониторингом
wipedisk.exe --maintenance --plan=quick_cleanup --verbose --monitor-resources

# Ожидаемый результат: отчет о потреблении ресурсов
```

---

## 🐛 **Обработка ошибок**

### 📋 **Проверка корректных exit codes**
```cmd
# Тестирование успешных операций
wipedisk.exe --maintenance --plan=quick_cleanup
echo %ERRORLEVEL%

# Ожидаемый результат: 0

# Тестирование операций с предупреждениями
wipedisk.exe --maintenance --plan=security_quarterly
echo %ERRORLEVEL%

# Ожидаемый результат: 2

# Тестирование критических ошибок
wipedisk.exe --maintenance --plan=invalid_plan
echo %ERRORLEVEL%

# Ожидаемый результат: 1
```

### 🔄 **Проверка восстановления после ошибок**
```cmd
# Тестирование прерывания операций
wipedisk.exe --maintenance --plan=quick_cleanup
# Прервать (Ctrl+C)
# Перезапустить ту же команду
# Проверить корректное восстановление
```

---

## 📝 **Логирование и отладка**

### 📋 **Проверка логирования**
```cmd
# Тестирование с verbose логированием
wipedisk.exe --maintenance --plan=quick_cleanup --verbose

# Проверка содержимого логов
type logs\wipedisk.log

# Проверка JSON логов
findstr /c:"ERROR" logs\wipedisk.log
```

### 🐛 **Проверка отчетов об ошибках**
```cmd
# Создание ошибки для проверки отчетов
wipedisk.exe --maintenance --plan=invalid_plan --verbose

# Проверка создания отчета об ошибке
dir reports\
```

---

## 🎯 **Критерии успешного тестирования**

### ✅ **Базовый функционал**
- [ ] Запуск без аргументов открывает интерактивное меню
- [ ] Все CLI команды работают корректно
- [ ] `--version` показывает полную информацию
- [ ] `--help` показывает справку
- [ ] Exit codes корректны (0=успех, 1=ошибка, 2=предупреждения)

### ✅ **Системное обслуживание**
- [ ] Очистка временных файлов работает
- [ ] Очистка DNS кэша работает
- [ ] Очистка корзины работает
- [ ] Планы обслуживания выполняются корректно
- [ ] Отчеты создаются в JSON формате

### ✅ **Затирание**
- [ ] Все методы затирания работают (quick/standard/secure/aggressive)
- [ ] Прогресс отображается корректно
- [ ] Защита от случайного удаления работает
- [ ] Верификация качества работает
- [ ] SSD обнаруживается корректно

### ✅ **Диагностика**
- [ ] Все уровни диагностики работают (quick/full/deep)
- [ ] Отчеты о диагностике создаются
- [ ] Тесты оборудования выполняются
- [ ] Системная информация собирается корректно

### ✅ **Безопасность**
- [ ] Проверка прав администратора работает
- [ ] Защита системного диска работает
- [ ] Zero-Trust I/O режим работает
- [ ] Все операции безопасны для системных файлов

### ✅ **Совместимость**
- [ ] Windows 7/10/11 поддерживаются
- [ ] x64 архитектура работает
- [ ] PowerShell скрипты работают
- [ ] Silent режим работает

### ✅ **Производительность**
- [ ] Операции выполняются в приемлемое время
- [ ] Память используется эффективно
- [ ] Нет утечек памяти
- [ ] Оптимизации для SSD работают

### ✅ **Отчеты**
- [ ] JSON отчеты создаются с правильной структурой
- [ ] TXT экспорт работает
- [ ] Управление отчетами работает
- [ ] Очистка старых отчетов работает

---

## 📋 **Документация результатов**

### 📝 **Формат отчета о тестировании**
Для каждого сценария создается отчет с информацией:
- Дата и время теста
- Версия WipeDisk
- Конфигурация системы
- Выполненные команды
- Результаты (успех/ошибка)
- Exit codes
- Замечания и рекомендации

### 📊 **Сбор метрик**
- Время выполнения операций
- Использование памяти
- Использование диска
- Количество созданных отчетов
- Размер отчетов

---

## 🚀 **Автоматизация тестирования**

### 🤖 **PowerShell скрипт для автоматического тестирования**
```powershell
# run_all_tests.ps1
$ErrorActionPreference = "Stop"

$testResults = @()

function Run-Test($testName, $command, $expectedExitCode = 0) {
    Write-Host "=== Тест: $testName ===" -ForegroundColor Green
    
    $startTime = Get-Date
    
    & $command
    
    $exitCode = $LASTEXITCODE
    
    $endTime = Get-Date
    $duration = ($endTime - $startTime).TotalSeconds
    
    $result = if ($exitCode -eq $expectedExitCode) { "SUCCESS" } else { "FAILED" }
    
    $testResults += @{
        Test = $testName
        Command = $command
        ExitCode = $exitCode
        Expected = $expectedExitCode
        Result = $result
        Duration = $duration
        Timestamp = $endTime
    }
    
    Write-Host "Результат: $result (Exit Code: $exitCode, Duration: ${duration}s)" -ForegroundColor $(if ($exitCode -eq $expectedExitCode) { "Green" } else { "Red" })
    Write-Host ""
}

# Запуск всех тестов
Run-Test "Interactive Menu" "wipedisk.exe" 0
Run-Test "CLI Help" "wipedisk.exe --help" 0
Run-Test "Version Info" "wipedisk.exe --version" 0
Run-Test "Quick Cleanup" "wipedisk.exe --maintenance --plan=quick_cleanup" 0
Run-Test "Diagnostics" "wipedisk.exe diagnose --quick" 0
Run-Test "Dry Run" "wipedisk.exe --wipe F: --dry-run" 0

# Генерация отчета
$reportPath = "test_results_$(Get-Date -Format 'yyyyMMdd_HHmmss').json"
$testResults | ConvertTo-Json -Depth 3 | Out-File -FilePath $reportPath -Encoding UTF8

Write-Host "=== Отчет сохранен: $reportPath ===" -ForegroundColor Cyan
```

---

## 🎯 **Рекомендации для тестирования**

### 📋 **Подготовка тестовой среды**
1. Использовать виртуальную машину с снапшотом
2. Создавать тестовые файлы и папки
3. Иметь достаточно свободного места на дисках
4. Использовать разные версии Windows для тестирования

### 📊 **Мониторинг во время тестирования**
1. Использовать Process Monitor для отслеживания
2. Проверять логи в реальном времени
3. Мониторить использование ресурсов
4. Проверять целостостность системы

### 🐛 **Проверка безопасности**
1. Тестировать на разных уровнях прав доступа
2. Проверять работу антивируса
3. Тестировать на системных дисках
4. Проверять защиту от случайного удаления

---

**WipeDisk Enterprise v1.3.0-stable готов к комплексному тестированию!** 🧪
